<div class="navbar">
      <%= link_to "Feed", posts_path %>
      <%= link_to "New post", new_post_path %>
      <a href="#">TBD</a>
        <div class="signout">
        <% if user_signed_in?%>
          <%= link_to current_user.email, destroy_user_session_path, data: { turbo_method: :delete } %>
        <% end %> 
        </div>
    </div>