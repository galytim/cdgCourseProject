<% if current_user.following?(user)%>
    <%= button_to "Unfollow", follow_path(user), method: :post, remote: true, id: "follow_button"%>
<% else-%>
    <%= button_to "Follow", follow_path(user), method: :post, remote: true, id: "follow_button"%>
<% end%>
